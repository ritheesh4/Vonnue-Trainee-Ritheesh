composition
-----------------------
Its a natural pattern of the component model.
In programming, composition allows you to build more complex functionality by combining small and focused functions.

Unidirectional Data Flow
-------------------------
The state is being passed from the parent to the child component and if there any action needs to be perfomed, that will execute.
Here the boarder colour of the input will change when user clicks the input field. It perfomed by the watching the state.

Explicit Mutations
-------------------------
You will always get the same output (in this case rendered DOM) no matter how many times you try with the same props